<li>
    {{burger_name}}
    {{log burger_name}}
    {{log devoured}}

{{#unless devoured}}
      <button class="devour-burger" data-id="{{id}}" data-devour="{{devoured}}">
        {{#unless devoured}}DEVOUR{{/unless}}
    </button>
    {{/unless}}
</li>